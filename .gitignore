# Environment files
.env
*.env.local

# Dependencies
node_modules/
**/node_modules/
**/.package-lock.json
**/.docusaurus/
docs-site/.docusaurus/
docs-site/build/

# Data directory (bind mount in Docker)
data/

# Crawlee / extractor runtime storage at repo root
storage/
# Re-include the deploy/ Terraform storage module (this is source code, not runtime data)
!deploy/modules/storage/

# Extractor storage outputs and cached auth
extractors/ukvisajobs/storage/

# OS files
.DS_Store
Thumbs.db

# local temp artifacts
tmp/

# Virtual environment for Python
.venv